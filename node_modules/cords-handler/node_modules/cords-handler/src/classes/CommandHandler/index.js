const { Collection } = require("discord.js");
const fs = require("fs");
const ascii = require("ascii-table");
const ms = require("ms");
const { NONAME } = require("dns");
const talkedRecently = new Set();
let table = new ascii("Commands");

table.setHeading("Command", "Load Status");

class CordsHandler {
  constructor(client, chclient, options) {
    if (!chclient)
      throw new ReferenceError("Cords-Handler Client Must Be Supplied.");
    if (!client) throw new ReferenceError("Cannot Find Client.");
    else {
      this.client = client;
      this.options = options;
      this.chclient = chclient;
      this.client.commands = new Collection();
      this.client.aliases = new Collection();
    }
  }
  setCommandFolder(cmdfolder) {
    if (!cmdfolder) throw new ReferenceError("Cannot Find Command Folder.");
    else {
      this.cmdfolder = cmdfolder;
    }
    return this;
  }
  setPrefix(prefix) {
    if (!prefix) throw new ReferenceError("Cannot Find Prefix.");
    else {
      this.prefix = prefix;
    }
    return this;
  }
  setPrefix2(prefix2) {
    if (!prefix2) return;
    else {
      this.prefix2 = prefix2;
    }
    return this;
  }
  setPrefix3(prefix3) {
    if (!prefix3) return;
    else {
      this.prefix3 = prefix3;
    }
    return this;
  }
  setPrefix4(prefix4) {
    if (!prefix4) return;
    else {
      this.prefix4 = prefix4;
    }
    return this;
  }
  setPrefix5(prefix5) {
    if (!prefix5) return;
    else {
      this.prefix5 = prefix5;
    }
    return this;
  }
  categorizeCommands(boolean) {
    if (boolean === false) return (boolean = false);
    if (boolean === undefined)
      throw new ReferenceError("Cannot Find True Or False.");
    else {
      this.categorize = boolean;
    }
    return this;
  }

  loader() {
    let komutlar = fs.readdirSync(`${process.cwd()}/${this.cmdfolder}`);
    for (let kategori of komutlar) {
      let komuts = fs.readdirSync(
        `${process.cwd()}/${this.cmdfolder}/${kategori}`
      );
      for (let komut of komuts) {
        let pull = require(`${process.cwd()}/${
          this.cmdfolder
        }/${kategori}/${komut}`);
        if (pull.name) {
          this.client.commands.set(pull.name, pull);
          table.addRow(`${kategori}/${komut}`, `✅`);
        } else {
          let data = {
            command: `${kategori}/${komut}`,
            message: "Cannot Read name.",
          };
          this.chclient.emit("catchError", data);
          table.addRow(`${kategori}/${komut}`, `❌ Cannot Read name.`);
          continue;
        }
        if (pull.aliases && Array.isArray(pull.aliases))
          pull.aliases.forEach((alias) =>
            this.client.aliases.set(alias, pull.name)
          );
        if (pull.cooldown) {
          if (!pull.cooldownError) {
            throw new ReferenceError(
              "Please Supply An Cooldown Error! In:" + file
            );
          } else {
            this.cooldown = pull.cooldown;

            this.cooldownError = pull.cooldownError;
          }
        }
      }
    }
    console.log(table.toString());
    this.chclient.emit("commandsLoaded");
    this.client.on("message", async (message) => {
      var p = this.prefix;

      if (message.content.startsWith(p)) {
        const commandName = message.content
          .slice(p.length)
          .trim()
          .split(" ")[0];
        const args = message.content.slice(p.length).trim().split(" ").slice(1);
        const cmd =
          this.client.commands.get(commandName) ||
          this.client.commands.get(this.client.aliases.get(commandName));

        if (cmd) {
          let data = {
            user: message.author,
            channel: message.channel,
            command: commandName,
          };
          if (cmd.cooldown) {
            if (talkedRecently.has(message.author.id)) {
              return message.channel.send(this.cooldownError);
            } else {
              talkedRecently.add(message.author.id);
              setTimeout(() => {
                talkedRecently.delete(message.author.id);
              }, ms(this.cooldown));
              cmd.run(this.client, message, args);
              this.chclient.emit("commandUsed", data);
            }
          } else {
            cmd.run(this.client, message, args);
            this.chclient.emit("commandUsed", data);
          }
        }
        if (!cmd) {
          let data = {
            command: commandName,
            user: message.author,
            channel: message.channel,
          };
          this.chclient.emit("commandNotFound", data);
        }
      }
      return this;
    });
    this.client.on("message", async (message) => {
      var p2 = this.prefix2;

      if (message.content.startsWith(p2)) {
        const commandName = message.content
          .slice(p2.length)
          .trim()
          .split(" ")[0];
        const args = message.content.slice(p2.length).trim().split(" ").slice(1);
        const cmd =
          this.client.commands.get(commandName) ||
          this.client.commands.get(this.client.aliases.get(commandName));

        if (cmd) {
          let data = {
            user: message.author,
            channel: message.channel,
            command: commandName,
          };
          if (cmd.cooldown) {
            if (talkedRecently.has(message.author.id)) {
              return message.channel.send(this.cooldownError);
            } else {
              talkedRecently.add(message.author.id);
              setTimeout(() => {
                talkedRecently.delete(message.author.id);
              }, ms(this.cooldown));
              cmd.run(this.client, message, args);
              this.chclient.emit("commandUsed", data);
            }
          } else {
            cmd.run(this.client, message, args);
            this.chclient.emit("commandUsed", data);
          }
        }
        if (!cmd) {
          let data = {
            command: commandName,
            user: message.author,
            channel: message.channel,
          };
          this.chclient.emit("commandNotFound", data);
        }
      }
      return this;
    });
    this.client.on("message", async (message) => {
      var p3 = this.prefix3;

      if (message.content.startsWith(p3)) {
        const commandName = message.content
          .slice(p3.length)
          .trim()
          .split(" ")[0];
        const args = message.content.slice(p3.length).trim().split(" ").slice(1);
        const cmd =
          this.client.commands.get(commandName) ||
          this.client.commands.get(this.client.aliases.get(commandName));

        if (cmd) {
          let data = {
            user: message.author,
            channel: message.channel,
            command: commandName,
          };
          if (cmd.cooldown) {
            if (talkedRecently.has(message.author.id)) {
              return message.channel.send(this.cooldownError);
            } else {
              talkedRecently.add(message.author.id);
              setTimeout(() => {
                talkedRecently.delete(message.author.id);
              }, ms(this.cooldown));
              cmd.run(this.client, message, args);
              this.chclient.emit("commandUsed", data);
            }
          } else {
            cmd.run(this.client, message, args);
            this.chclient.emit("commandUsed", data);
          }
        }
        if (!cmd) {
          let data = {
            command: commandName,
            user: message.author,
            channel: message.channel,
          };
          this.chclient.emit("commandNotFound", data);
        }
      }
      return this;
    });
    this.client.on("message", async (message) => {
      var p4 = this.prefix4;

      if (message.content.startsWith(p4)) {
        const commandName = message.content
          .slice(p4.length)
          .trim()
          .split(" ")[0];
        const args = message.content.slice(p4.length).trim().split(" ").slice(1);
        const cmd =
          this.client.commands.get(commandName) ||
          this.client.commands.get(this.client.aliases.get(commandName));

        if (cmd) {
          let data = {
            user: message.author,
            channel: message.channel,
            command: commandName,
          };
          if (cmd.cooldown) {
            if (talkedRecently.has(message.author.id)) {
              return message.channel.send(this.cooldownError);
            } else {
              talkedRecently.add(message.author.id);
              setTimeout(() => {
                talkedRecently.delete(message.author.id);
              }, ms(this.cooldown));
              cmd.run(this.client, message, args);
              this.chclient.emit("commandUsed", data);
            }
          } else {
            cmd.run(this.client, message, args);
            this.chclient.emit("commandUsed", data);
          }
        }
        if (!cmd) {
          let data = {
            command: commandName,
            user: message.author,
            channel: message.channel,
          };
          this.chclient.emit("commandNotFound", data);
        }
      }
      return this;
    });
    this.client.on("message", async (message) => {
      var p5 = this.prefix5;

      if (message.content.startsWith(p5)) {
        const commandName = message.content
          .slice(p5.length)
          .trim()
          .split(" ")[0];
        const args = message.content.slice(p5.length).trim().split(" ").slice(1);
        const cmd =
          this.client.commands.get(commandName) ||
          this.client.commands.get(this.client.aliases.get(commandName));

        if (cmd) {
          let data = {
            user: message.author,
            channel: message.channel,
            command: commandName,
          };
          if (cmd.cooldown) {
            if (talkedRecently.has(message.author.id)) {
              return message.channel.send(this.cooldownError);
            } else {
              talkedRecently.add(message.author.id);
              setTimeout(() => {
                talkedRecently.delete(message.author.id);
              }, ms(this.cooldown));
              cmd.run(this.client, message, args);
              this.chclient.emit("commandUsed", data);
            }
          } else {
            cmd.run(this.client, message, args);
            this.chclient.emit("commandUsed", data);
          }
        }
        if (!cmd) {
          let data = {
            command: commandName,
            user: message.author,
            channel: message.channel,
          };
          this.chclient.emit("commandNotFound", data);
        }
      }
      return this;
    }); 
    return this;
  }
}
module.exports = CordsHandler;
